spring:
    
  datasource:
      # url: jdbc:postgresql://${PRODUCT_PG_HOST:localhost}:${PRODUCT_PG_PORT:5432}/${PRODUCT_PG_DATABASE:postgres}
      # username: ${PRODUCT_PG_USERNAME:root}
      # password: ${PRODUCT_PG_PASSWORD:root}
      url: jdbc:postgresql://localhost:5432/postgres
      # username: ${PRODUCT_PG_USERNAME:root}
      # password: ${PRODUCT_PG_PASSWORD:root}
      username: root
      password: root
      driver-class-name: org.postgresql.Driver
  jpa:
      hibernate:
        ddl-auto: update
      # database: postgresql
      # database-platform: org.hibernate.dialect.PostgreSQLDialect
      # show-sql: true
      properties:
        hibernate:
          dialect: org.hibernate.dialect.PostgreSQLDialect

server:
  port: ${PRODUCT_SERVICE_PORT:8345} 

    # datasource:
  #   url: jdbc:postgresql://${PRODUCT_PG_HOST:localhost}:${PRODUCT_PG_PORT:5432}/product
  #   username: ${PRODUCT_PG_USERNAME:root}
  #   password: ${PRODUCT_PG_PASSWORD:root}
  #   driver-class-name: org.postgresql.Driver
  # jpa:
  #   hibernate:
  #     ddl-auto: validate
  #   database: postgresql
  #   database-platform: org.hibernate.dialect.PostgreSQLDialect
  # flyway:
  #   baseline-on-migrate: true
  #   baseline-description: "init"
  #   baseline-version: 0
  #   enabled: true
  #   user: ${spring.datasource.username}
  #   password: ${spring.datasource.password}
  #   locations: classpath:db/migration